# AI-VOICE-ASSISTANT

## Subnet Ping Drone

This app scans a subnet and reports which hosts respond to a ping.

### Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open `http://localhost:5000` in your browser.
